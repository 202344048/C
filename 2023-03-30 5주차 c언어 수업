#2023-03-30 5주차 c언어 수업
#include<stdio.h>

int main(void)
{
  int a, b;

  printf("정수 두개를 입력하시오:");
  scanf("%d %d", &a, &b);

  if(a>b)
  {printf(">를 출력한다.");}

  else if(a<b)
  {printf("<를 출력한다");}

  else if(a==b)

  {printf("==를 출력한다");}

  return 0;
}
