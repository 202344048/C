#include<stdio.h>
int main(void)
{
  double x, y, z;
  double sum, average;

  printf("3개의 실수를 입력하시오 : ");
  scanf("%lf %lf %lf", &x, &y, &z);

  sum = x + y + z;
  average = sum/3;

  printf("합계 = %.2lf\n", sum);
  printf("평균 = %.2lf\n", average);

  return 0;
}
